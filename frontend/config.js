// Configuração centralizada de ambiente da plataforma Estudo Intenso
const CONFIG = {
    // Enquanto testa no computador, mantenha o localhost. 
    // Quando fizer o deploy, mude para a URL gerada (Ex: https://meu-backend.onrender.com)
   // Agora o seu front-end aponta direto para o servidor real na internet!
    URL_BACKEND: "https://estudo-intensivo-backend.onrender.com"
};